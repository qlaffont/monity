language: node_js

node_js:
  - '12'

services:
  - docker
  - mongodb

install:
  - yarn install --silent

before_script:
  - sleep 15

script:
  - npm run test

deploy:
  provider: script
  script: bash deploy.sh
  on:
    branch: master
